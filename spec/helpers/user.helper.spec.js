const should = require('should');

const helper = require('../../helpers/user.helper');

describe('helper', () => {
    it('should ...', () => {
        (helper.multiply(3)).should.be.equal(9);
    });
});
